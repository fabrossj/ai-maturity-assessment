# Docker Environment Configuration
# Copy this file to .env before running docker-compose

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-change-in-production-use-openssl-rand-base64-32

# Email Configuration (Nodemailer)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@example.com

# Notes:
# - Database and Redis URLs are configured in docker-compose.yml
# - For production, generate a secure NEXTAUTH_SECRET: openssl rand -base64 32
# - For Gmail, use an App Password (not your regular password)
# - All other environment variables are set in docker-compose.yml
