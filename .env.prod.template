# Production Environment Configuration
# Copy this file to .env.prod and configure with your production values
# Usage: docker-compose -f docker-compose.prod.yml --env-file .env.prod up -d

# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_THIS_STRONG_PASSWORD
POSTGRES_DB=ai_assessment

# Redis Configuration
REDIS_PASSWORD=CHANGE_THIS_REDIS_PASSWORD

# Upstash Redis (for rate limiting)
# If using Upstash, get these from https://console.upstash.com/
# If using local Redis, these will be ignored
UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-upstash-token

# NextAuth Configuration
NEXTAUTH_URL=https://yourdomain.com
NEXTAUTH_SECRET=GENERATE_WITH_openssl_rand_base64_32

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-production-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@yourdomain.com

# Security Notes:
# 1. Generate NEXTAUTH_SECRET: openssl rand -base64 32
# 2. Use strong passwords (min 16 characters, alphanumeric + symbols)
# 3. For Gmail: Use App Password, not regular password
# 4. Never commit this file to version control
# 5. Store production secrets in a secure vault (AWS Secrets Manager, etc.)
